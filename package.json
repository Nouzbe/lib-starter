{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start --parallel --include-filtered-dependencies --scope bam-components --scope bam-storybook"
  },
  "devDependencies": {
    "lerna": "^3.5.1"
  }
}
